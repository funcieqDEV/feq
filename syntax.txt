ext fn puts(buf: *char) -> int;

fn main(argc: int, argv: **char) -> int {

    a: int = 5; 


    if true {

    }

    # we "borrow" it from odin

    for {
        //infinite loop
        break;
        //not that infinite cuz there is break;
    }

    a: [10]int = {1,2,3,4,5,6,7,8,9,0};

    puts("hello world");
    ret 0;
}
namespace Src.Sema;
using Src.AST;

public class SemanticChecker
{
    //im too lazy to implement this rn, so ill just return the root node for now
    public RootNode Check(RootNode root)
    {
        return root;
    }
}